# Server Configuration
NODE_ENV=development
PORT=5000

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# ============================================================
# PostgreSQL Database (for Tamper-Evident Notice Chain)
# ============================================================

# Option 1: Full connection URL
DATABASE_URL=postgresql://neondb_owner:npg_GC7yXcOHJu0Y@ep-dry-flower-aos1ax9s.c-2.ap-southeast-1.aws.neon.tech/neondb?sslmode=require

# Option 2: Individual parameters (used if DATABASE_URL not set)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_secure_password
DB_NAME=noticeledger

# ============================================================
# Blockchain Configuration (for smart contract reading)
# ============================================================

# RPC Provider URL
RPC_URL=https://sepolia.infura.io/v3/YOUR_INFURA_KEY

# Ethereum Network
ETHEREUM_NETWORK=sepolia

# Smart Contract Address
CONTRACT_ADDRESS=0x5FbDB2315678afccb333f8a9c6122f65385ba4c8a

# API Keys (optional)
ALCHEMY_API_KEY=your_alchemy_key
ETHERSCAN_API_KEY=your_etherscan_key

# JWT (if using authentication)
JWT_SECRET=your_secret_key_here


# API Keys (if needed)
OPENAI_API_KEY=your_key

