
- 
+ # Server Configuration
+ PORT=3001
+ BACKEND_PORT=3001
+ 
+ # Frontend URL (for CORS)
+ FRONTEND_URL=http://localhost:5173
+ 
+ # ============================================================
+ # PostgreSQL Database Configuration
+ # ============================================================
+ 
+ # Full connection URL (recommended)
+ DATABASE_URL=postgresql://postgres:postgres@localhost:5432/blocknotice
+ 
+ # Admin wallet address (for faculty-only access)
+ ADMIN_WALLET_ADDRESS=0xYourFacultyWalletAddressHere
+ 
- 
+ # Server Configuration
+ PORT=3001
+ BACKEND_PORT=3001
+ 
+ # Frontend URL (for CORS)
+ FRONTEND_URL=http://localhost:5173
+ 
+ # ============================================================
+ # PostgreSQL Database Configuration
+ # ============================================================
+ 
+ # Full connection URL (recommended)
+ DATABASE_URL=postgresql://postgres:postgres@localhost:5432/blocknotice
+ 
+ # Admin wallet address (for faculty-only access)
+ ADMIN_WALLET_ADDRESS=0xYourFacultyWalletAddressHere
+ {
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.2.3",
    "ethers": "^6.16.0",
    "express": "^5.2.1",
    "helmet": "^8.1.0",
    "mongoose": "^9.1.5",
    "pg": "^8.11.0"
  }
}
